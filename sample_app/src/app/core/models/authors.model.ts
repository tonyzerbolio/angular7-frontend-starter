/**
 * AuthorsResult is a model describing the data that is
 * used in the authors service.
 */
export class AuthorsResult {
    id?: number;
    firstName?: string;
    lastName?: string;
    age?: number
}
