apiVersion: v1
kind: Service
metadata:
  annotations:
    author: Levon Becker
  labels:
    comet.service: angularjs
  name: angularjs
  namespace: comet
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    comet.service: angularjs
